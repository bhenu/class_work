#include<stdio.h>

int main(int argc, char * argv[])
{
    char exp[] = argv[1];   /* store the input expression */
    char s[20];             /* temporary variable to store the integers */
    int i = 0, j;               /* index position for input string */
    while ((c = exp[i++]) == ' ' || c == '\t')
        ;
    if (!isdigit(c) && c != '.')
        /* operator found! do the operation and push */
        ;
    i = 0;
    if (isdigit(s[i++] = c = ))

    return 0;
}

